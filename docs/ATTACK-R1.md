# ATTACK BATTERY R1 — findings ledger (2026-08-19, instance-tier, 5 lenses + synthesis, 0 void)


## lens: ?  (seed_hit=True, off_map=~40% (finding 1 is on-seed S2; finding 2 on the engineer/evidence-dossier gate, plus both flags on the intake gate and the O2 options_rejected guard, were hunted off-seed))

### [P1] src/validators/assemble_manifest.py derive_verdicts() lines 304-308 (the only lens-count floor); src/validators/validate_decision.py validate() lines 92-119 (never reads the lens artifacts); tools/reverify.mjs verdictFoldError() lines 295-303 + batteryFindingsCounts schema line 106-116; src/manifest/pipeline.manifest.json lines 133-146 (fanout: 3 lenses, no rigor floor)
- claim: The acceptance chain has NO floor forcing battery rigor. battery_verdict is a self-reported scalar authored by the synthesis subagent, and the ONLY mechanical consistency check on it is one-directional: assemble_manifest refuses 'breaches_found with zero counted findings'. The permissive direction — battery_verdict=='clean' while the battery-lens-*.json artifacts carry P1/P2 findings — is checked by NOTHING (validate_decision does not read lenses; assemble_manifest has no clean-branch; reverify.mjs recomputes the fold from the WRITTEN battery value and only fails effective>fold). Because clean is the sole verdict whose cap is 'industrial', a lazy/gaming synthesis (or three hollow lenses that record findings:[]) rubber-stamps clean and ships effective=industrial, and every downstream 'mechanical guarantee' (T-D5 'evidence cannot be faked') certifies it. The SEED anti-false-negative gate that is supposed to force each lens to surface its planted defect is pure persona-text: grep of packages/dsh-pipeline-executor/lib/*.js for 'seed' returns 0 hits — the executor never enforces seed-hit, never voids a zero-finding lens run, never cross-checks clean against lens severities. This is the exact 'green-but-wrong' defect the battery exists to catch, un-guarded in the direction that unlocks the top tier.
- repro: Craft workspace (files under scratchpad/cheat-ws): build/package.json {name:dsh-cheat-plugin,version:0.1.0} + build/evals/run_harness.sh (exit 0); artifacts/decision-record.json with capability_level=O-L3, one machine-adjudicated gate with a boilerplate options_rejected, acceptance={re_audit_verdict:industrial, battery_verdict:clean, battery_independence_tier:model, battery_stop_reason:'E9...', effective_verdict:industrial}; artifacts/evidence-dossier.json with one E-L0 green layer + verification.harness_path=evals/run_harness.sh; artifacts/battery-lens-gaming.json = {findings:[{lens:gaming,location:'SKILL.md',claim:'core acceptance check bypassable',reproduction:{...},severity:'P1'}],flags:[]}; evidence-ledger.jsonl with two lines sharing one manifestSha256 and roleModel=deepseek-v4-pro. Then run the battery gate exactly as the manifest declares it: (1) python3 src/validators/validate_decision.py artifacts/decision-record.json ; (2) python3 src/validators/assemble_manifest.py --workspace WS --build-subdir build --out artifacts/acceptance-manifest.json --provider deepseek-official --dsh-root /nonexistent ; (3) cp the manifest into build/ and node tools/reverify.mjs build --skip-commands.
- expected: A record asserting battery_verdict==clean while a lens artifact recorded a P1 breach should be refused (a 'green but visibly wrong' battery is breaches_found, NEVER clean — battery.md Contract), OR effective==industrial should be blocked because a P1 is counted. The mechanical assembler exists precisely to refuse fraud classes where the shipped verdict is not supported by the evidence.
- observed: STEP 1 validate_decision.py -> 'PASS' exit=0. STEP 2 assemble_manifest.py -> 'PASS ... effective=industrial' exit=0, writes acceptance-manifest.json. STEP 3 reverify.mjs -> 'reverify: PASS (8/8 checks)'. The shipped acceptance-manifest.json carries verdicts={battery:'clean', effective:'industrial', batteryFindingsCounts:{p1:1,p2:0,p3:0}} — a self-contradicting record (a P1 breach counted IN the shipped evidence, yet certified clean at the top tier) that all three mechanical checks bless.

### [P3] src/validators/validate_report.py validate() lines 96-121 (evaluator_calibration honesty checks); consumed at pipeline.manifest.json stage 'engineer' gate
- claim: The evidence-dossier gate's evaluator-honesty layer is unverifiable self-report: has_unknown_exit==true, different_source_from_builder==true, model_baseline non-empty, golden_sample_count>0, and a style/verbosity substring in audit_dimensions are all fields the BUILDER writes about its own evaluator, and validate_report only checks their SHAPE, never their truth. A gaming engineer that grades itself with its own judge writes different_source_from_builder:true (a literal falsehood), golden_sample_count:1, model_baseline:'<any text>', audit_dimensions:['style/verbosity bias'] and passes. Migration test: swap the skill name for any sibling's and the identical values still pass -> boilerplate. The design's stated backstop is the battery's evaluator-audit rotation (roles/battery.md 'Author-homology of consumed upstream fields'), but that backstop is the same floorless self-reporting mechanism exposed in finding 1 — so the independence claim has no real check.
- repro: Build scratchpad/dossier-cheat: run_harness.sh (exit 0, chmod +x); red.log with any one non-empty line; evidence-dossier.json with E-L0..E-L4 green layers, evaluator_calibration=[{evaluator:'my-own-judge',evaluator_kind:'llm_judge',golden_sample_count:1,model_baseline:'whatever-i-type',has_unknown_exit:true,different_source_from_builder:true,audit_dimensions:['style/verbosity bias (not actually audited)']}], red_light_history{red_artifact_path:'red.log',red_before_green:true}, stop_conditions all non-empty, verification{harness_path:'run_harness.sh',all_required_passed:true,exit_code:0}. Run: python3 src/validators/validate_report.py evidence-dossier.json --target-dir scratchpad/dossier-cheat.
- expected: A dossier whose evaluator independence/unknown-exit/baseline fields are self-asserted by the builder (here the builder grading itself with 'my-own-judge' yet claiming different_source_from_builder:true) should not be able to pass an honesty gate on shape alone; independence is a property the graded party cannot self-certify.
- observed: validate_report.py -> 'PASS' exit=0. Every honesty boolean/string was accepted without any verification; the harness re-run (the one real check) trivially exits 0.

- FLAG @ src/preset/charter.md Step 0.5 lines 26-28 ('PASS immediately if it contains the line # TOOL-CREATOR SPEC v1'): Existence-check bypass of the intake gate: the gate PASSES IMMEDIATELY on the mere presence of the literal string '# TOOL-CREATOR SPEC v1', skipping the (a)/(b)/(c) component count entirely. A lazy actor pastes that header above any garbage request and the minute-cost pre-filter is defeated (the hour it exists to save is paid downstream). Flagged not-a-finding because the gate is conductor-MODEL-executed (no runnable validator to exit non-zero) and the charter itself routes real correction to the composer gate — the spirit that fails is only the early-abort optimization, not the acceptance guarantee. Note this is adjacent to the S1 (coherence) seed site; reported here purely as the Gaming existence-check reading.

- FLAG @ src/validators/validate_decision.py validate() lines 83-84; schema src/schemas/decision-record.json options_rejected (line 28, 'empty rejected list = un-thought signal'): The O2 'un-thought signal' guard (verdict==pass requires non-empty options_rejected) is a pure existence check satisfied by one boilerplate entry, e.g. [{option:'hold',why:'no blocking findings judged material'}] — used verbatim in the finding-1 repro and it passed. Migration test survives (name-agnostic). Low severity / self-labeled soft signal by O2, but it means every gate 'decision object' can be a template with zero real deliberation. Same self-report root as finding 1.

## lens: ?  (seed_hit=True, off_map=3 of 4 findings (75%) are off-seed — the manifest synthesis 24576-vs-32768 same-file numeric drift, the battery role 'default all five' vs manifest 3-lens drift, and the plugin §2.1 'these two invariants' stale count — plus 2 off-seed flags (T-D7 −47% arithmetic, artifact_root mis-location). Roughly 70% of effort went to non-seed hunting. Numeric-budget multiplication performed and reported: trigger battery (20 pos x 90% = 18 must pass; 10 neg x 5% = 0 false fires allowed — satisfiable); retries=2 -> 3 attempts matches the charter attempt table (never attempt 4); battery budget 3x12288 lens + synthesis (61440 with the comment's 24576, 69632 with the field's 32768) vs engineer 65536 — the '~engineer level' rationale only closes on the stale 24576, which is finding #2; verdict min-fold (draft<candidate<industrial) is consistent across charter, battery prompt, and validate_decision.py — no arithmetic contradiction there.)

### [P2] src/preset/charter.md Step 0.5 (lines 33-41) vs src/skills/spec-grill/SKILL.md (checklist item 3, line 32; SPEC skeleton '## 触发与使用', line 47)
- claim: Definition/target-awareness drift on the (c) 'how it is used' component: the charter's intake gate is target-aware and accepts 'any one form' of (c) — a trigger phrase OR an operating domain — and explicitly states 'a preset has no trigger phrase; it is invoked by running under it.' spec-grill (the charter's own 'preferred entry') is NOT target-aware: its coverage checklist and SPEC skeleton require >=2 positive trigger phrases plus >=1 negative trigger for EVERY grilled spec, and provide no operating-domain field at all. A preset spec cannot honor both docs.
- repro: Grill a PRESET via spec-grill (the entry the charter steers users to). Item 3 ('触发与使用:正向触发短语/场景 >=2,反向(不该触发) >=1') and the skeleton section '## 触发与使用 <正向触发 >=2;反向触发 >=1>' require >=2 positive + >=1 negative trigger, and 'covering checklist 全勾才算饱和' (line 29) makes item 3 mandatory for saturation. Now read charter Step 0.5 lines 36-37: 'a preset has no trigger phrase; it is invoked by running under it', with an operating domain as the sole (c). The SPEC skeleton has no operating-domain section anywhere. So the preset's defining (c) form has no home in the output block, and its inapplicable form is required.
- expected: The two docs agree, target-aware, on what a complete preset spec's (c) component is: an operating domain, with no trigger required (matching the charter's stated rule that a preset has no trigger phrase).
- observed: spec-grill's saturation checklist is unsatisfiable for a preset without inventing phantom trigger phrases the charter says presets do not have; the two shipped docs give contradictory definitions of a complete preset spec's (c). Blast radius is bounded (the intake gate PASSES immediately on the '# TOOL-CREATOR SPEC v1' header without re-checking components, per charter line 27-28, and phantom triggers are inert for a preset target whose harness has no trigger battery), so it produces no wrong PASS/REFUSE — the harm is that a whole first-class target class is mis-served at the pre-pipeline interrogation layer.

### [P3] src/manifest/pipeline.manifest.json:134 (battery fanout $comment) vs :152 (stages[battery].fanout.synthesis.maxTokens)
- claim: Same-file numeric contradiction on the synthesis token budget: the fanout $comment states 'synthesis keeps 24576' but the synthesis role field is maxTokens: 32768.
- repro: grep -n '24576\|32768' src/manifest/pipeline.manifest.json -> line 134 '...synthesis keeps 24576 (it emits the full decision record — starving it reproduces the guidance-class death)'; line 152 '"maxTokens": 32768'. Cross-check the comment's own rationale ('Battery scale is budgeted to ~engineer level'; engineer maxTokens=65536, line 76): 3 lenses x 12288 + 24576 synthesis = 61440 (<= engineer, consistent), but with the actual 32768 synthesis = 69632 (> engineer). The '~engineer level' arithmetic only closes on the comment's stale 24576.
- expected: The $comment and the synthesis.maxTokens field state the same number; the executor's actual synthesis budget matches its documented rationale.
- observed: Comment says 24576, field is 32768; the executor allocates 32768 (the field), so the documented budget and the 'budgeted to ~engineer level' justification are both wrong for the shipped value. A reader auditing the battery cost model from the comment computes the wrong total.

### [P3] src/roles/battery.md:26 (Contract: lenses[]) vs src/manifest/pipeline.manifest.json:134-139 (fanout default lenses)
- claim: Layer desync on the default battery composition: the battery role charter (delivered AS persona to every lens and to synthesis) says 'lenses[] — subset of the five (default: all five + synthesis)', but the manifest's operative default is THREE lenses (coherence/gaming/reality), with evidence+foundation opt-in.
- repro: roles/battery.md:26 -> 'default: all five + synthesis' (reinforced by line 63 'The five lenses (minimal spanning set — do not invent a sixth)' and the SEED gate listing all five). pipeline.manifest.json:134 -> 'THREE default lenses (coherence/gaming/reality ...); evidence and foundation are opt-in', :135-139 -> lenses = [coherence, gaming, reality]. The executor fans out per the manifest (3), while the persona each dispatched lens/synthesis reads asserts the default is five.
- expected: The shipped persona and the manifest agree on the default lens set (both 3, or both 5).
- observed: The authoritative default is 3 (manifest), but the persona claims all-five is the default; a synthesis pass reading its own charter is told a battery it did not run is the standard one, which can suppress an honest coverage_gaps entry for the two unrun lenses (evidence/foundation) — the very lenses that catch the staleness/false-axiom classes the differential claims lean on.

### [P3] src/targets/plugin/BUILD.md:204 (§2.1 closing 'Verify before you claim done') vs the §2.1 invariant headings (lines 98, 130, 146, 156, 176, 188)
- claim: Stale self-referential count: §2.1's closing sentence says 'these two invariants are precisely the failures such a green would hide', but §2.1 now lists SIX invariants (1 through 6).
- repro: grep -n 'Invariant [0-9]\|these two invariants' src/targets/plugin/BUILD.md -> six 'Invariant N —' headings at 98/130/146/156/176/188, but line 204 reads 'these two invariants'. The parallel sentence in the sibling pack (src/targets/preset/BUILD.md:134) correctly says 'these invariants' with no stale count, confirming line 204 was left unupdated when invariants 3-6 were appended.
- expected: The §2.1 summary references all six invariants (or is count-agnostic like the preset pack's wording).
- observed: 'these two invariants' is stale from when only Invariants 1-2 existed; an engineer reading it may believe only two invariants (Config-schema, peerDependencies) are the E-L4-hidden failures, understating the four later ones (additionalProperties, optional-peer, execute-return-shape, deep-frozen-result) that the pack elsewhere marks as live-boot-only.

- FLAG @ docs/evidence/differential-battery.md:15 (T-D7) and src/manifest/pipeline.manifest.json:134: T-D7 claims '5 透镜 464-495K/次 -> 3 透镜 243-259K/次(−47%)'. Removing 2 of 5 lenses is a 40% count cut; a 47% cost cut requires the two removed lenses (evidence, foundation) to be the heaviest (plausible — they carry web_search and read more), which means the −47% is specific to WHICH lenses were dropped, not a general '3/5 of the cost' proportional law. If per-lens cost were uniform with any positive shared/synthesis overhead F (F + 5L ~= 479K, F + 3L ~= 251K), solving gives L ~= 114K and F ~= -91K — an impossible negative overhead — so the numbers are only coherent under strongly non-uniform lens costs. This is an Evidence-lens measurement question (needs the r1c/R2/R3 session token logs), not a self-contradiction I can exhibit; flagged, not a finding.

- FLAG @ src/validators/assemble_manifest.py:404-417 (artifact_root): artifact_root returns build_dir whenever build_dir carries ANY marker (package.json/preset.yml/agent.cordis.yml/SKILL.md), and only steps down to build/preset/ when build_dir has NO marker. For a well-formed preset the top level has no marker so build/preset/ wins; but a malformed preset that leaves a stray marker at build/ root (e.g., a SKILL.md or preset.yml accidentally placed there) would make the manifest hash the workspace-level tree instead of the install unit build/preset/, silently mis-locating the artifact_root. This is the 'both markers — which wins?' case (S4/Reality territory); I can describe the mis-location but not exhibit a self-contradiction double-bind, so it is a flag for the Reality lens to prove.

## lens: ?  (seed_hit=True, off_map=~35% — beyond the seed's O-L3/veto site I audited the acceptance-manifest+reverify chain's decoupling from the capability claim (ran current validate_decision on the historical r1c O-L1+machine record -> REJECT, while reverify on r1c's shipped build -> PASS 86/86, proving the shipped evidence spine is independent of capability validity), the differential-battery.md T-D1/T-D5 claims and the closing evidence table (Flags A,C), the assembler's asymmetric battery-findings guard (Flag C), and the battery independence-tier premise (Flag B). Confirmed the mechanical spine (disk-byte sha256, verdict min-fold recompute, ledger sha pin, symlink refusal) genuinely holds — the fold and hash guarantees are sound; the defect is the governance self-label, not the mechanics.")

### [P2] src/manifest/pipeline.manifest.json L5-6 (capabilityLevel "O-L3", "structurally fixed") vs src/references/orchestration-anchors.md §4 L69-81 + src/schemas/decision-record.json L12 + src/validators/validate_decision.py L52,L85 (HUMAN_JUDGED_LEVELS)
- claim: O-L3 is an UNEARNED birth-constant that contradicts the pipeline's own O7 capability-ladder doctrine. The ladder is defined as an EARNED progression — decision-record schema L12: "O7: ships at O-L0 (every gate human-judged); upgrades pre-registered"; anchors §4: "Upgrade conditions are pre-registered ... Autonomy is bought by the Evidence Dossier, not by demo polish. Any serious incident auto-demotes one level." But the pipeline is born O-L3 by a manifest constant that the executor stamps mechanically, with no climb, no pre-registered condition met, and no human ever in the loop to calibrate against. Worse, the doctrine's own starting rung is UNREACHABLE: because every gate in a headless run is machine-adjudicated, validate_decision's machine-factory invariant REJECTS O-L0/O-L1/O-L2 and accepts only O-L3/O-L4. So 'ship at O-L0' is illegal for the only kind of record the pipeline produces; O-L3 is the LOWEST label the validator tolerates — a forced floor, not an earned level. The manifest comment even admits the honest low label was punished: 'R2 wrote O-L0 (validator-rejected -> retries -> stopped_unmet), R3 wrote O-L3 (passed)'. And 'veto reserved to human' names a safety net that structurally cannot exist headless (no pause point, no review queue, no human). The field reports 'the value that passes the gate', not measured capability.
- repro: cd /Users/vince/playground/dsh-tool-creator; run: python3 - <<'PY'  building a decision-record whose gates all carry adjudicator="machine" (the headless reality), and labelling it O-L0..O-L4 in turn, calling src/validators/validate_decision.py on each. Observed output: O-L0->REJECT, O-L1->REJECT, O-L2->REJECT, O-L3->PASS, O-L4->PASS. Then quote decision-record.json L12 'O7: ships at O-L0'. The doctrine (ship at O-L0, earn upgrades) and the enforced invariant (machine record must be O-L3+) directly contradict.
- expected: If the O-series ladder is earned as its own O7 doctrine states, a fresh pipeline ships at O-L0 and climbs via pre-registered upgrade conditions (N rounds zero human/machine disagreement); the capability_level field reflects measured autonomy maturity and a real, exercisable human veto point.
- observed: The pipeline is born O-L3 by a hard-coded manifest constant, mechanically stamped over whatever the battery wrote; O-L0/L1/L2 are validator-illegal for its (all-machine) records, so the doctrine's 'ship at O-L0' can never happen; O-L3 was mandated specifically because it is the lowest label validate_decision accepts. 'human veto reserved to human' is aspirational — no human is in a headless run at any point. O-L3 is a fig leaf: a forced floor dressed as an earned mid-ladder trust level with a human safety net that does not exist.

### [P2] src/validators/assemble_manifest.py (derive_limits L361-371 + limits assembly L638) + src/schemas/acceptance-manifest.json (no capability_level/adjudicator field) + docs/ACCEPTANCE-MANIFEST.md 'Honest limits' L77-91; promise site: orchestration-anchors.md §4 L74 + manifest/prompts/battery.md L30
- claim: The doctrine repeatedly promises the un-exercised human veto / machine-only adjudication is 'disclosed as a limit' (anchors §4 L74 'human veto is reserved but not exercised in a headless run, disclosed as a limit'; battery.md L30 'Record the un-exercised human veto as a coverage limit'). But that disclosure NEVER reaches the shipped artifact. The acceptance-manifest.json — the 'evidence that travels with the artifact', the file a dsh-suite directory re-verifies — has no capability_level/adjudicator/veto field at all, and assemble_manifest's mechanically-derived limits[] is only (dossier not_run layers)+(kit staging)+(dsh-unknown)+(ledger-timing); it can never emit an adjudication-mode limit. The disclosure lives only in the pipeline-internal decision-record.json, which never ships and is never re-verified — and even there it is applied non-deterministically (the very model-compliance dependency the stamp was meant to kill: the stamp fixed the LEVEL but not its LIMIT disclosure). A consumer reading a shipped manifest (effective=candidate/industrial, battery=clean/breaches_found, model=deepseek-v4-pro) cannot learn the acceptance was produced by a machine battery self-adjudicating with zero human oversight.
- repro: 1) grep -riE 'capability|adjudicat|veto|human|O-L3' on the three live shipped manifests build/acceptance-manifest.json (r1c skill, r2d plugin) and build/preset/acceptance-manifest.json (r3 preset) under /Users/vince/playground/tool-creator-runs -> zero governance/adjudication hits (only a 'capability-uplift' substring inside a baselineDelta note). 2) python3 dump each manifest's limits[] -> only E-Lx not_run + kit-staging + ledger-timing lines; no adjudication/veto line. 3) Read assemble_manifest.py limits assembly (L638): no code path emits it. 4) Contrast decision-record.json blobs: r1c mentions veto=False, r2d veto/human=False, only r3 mentions veto — inconsistent even internally.
- expected: Per the doctrine and docs/ACCEPTANCE-MANIFEST.md's 'prefers a truthful gap' honesty claim, the shipped honest-limits list should carry a standing machine-adjudication / un-exercised-human-veto disclosure so a downstream re-verifier learns no human adjudicated or could veto this artifact.
- observed: All three live shipped manifests omit it entirely; the assembler cannot produce it; the disclosure exists only in the non-shipped decision-record.json and is written there only ~1/3 of the time. r1c further proves the decoupling: its decision-record (O-L1 + machine adjudicators) is REJECTED by current validate_decision, yet its shipped build passes reverify 86/86 — the shipped evidence chain is fully independent of the capability claim's validity.

- FLAG @ docs/evidence/differential-battery.md L38-40 (closing '三 target live 闭环' table): The closing evidence table lists 'O-L3' and 'O-L3 首过' (O-L3 passed on first try) as 关键证据 (key evidence) for r3/r2d. This conflates a stamped, unconditional constant with an earned/measured achievement: since the executor stamps capabilityLevel=O-L3 into every record regardless of model output, 'O-L3 首过' is vacuous — there is nothing to pass. Presenting the forced floor as a proven capability overstates what the run demonstrated.

- FLAG @ battery independence: pipeline.manifest.json fanout (lensPersona roles/battery.md) + acceptance battery_independence_tier field: The 'independent acceptance battery' shares its base model (deepseek-v4-pro) with the very roles it audits, dispatched by the same executor in the same run; independence is model/instance-tier at best. A shared-model blind-spot class (a defect all deepseek-v4-pro instances miss) is structurally invisible to every lens and to the synthesis — the battery cannot be more independent than the model boundary it never crosses. Partially disclosed via battery_independence_tier, but the 'independent' framing overstates the independence actually reachable, and survivorship of N green runs on one model/author cannot see it.

- FLAG @ src/validators/assemble_manifest.py derive_verdicts L304-308: The battery-vs-findings integrity guard is one-directional: it refuses battery_verdict=='breaches_found' with zero counted lens findings, but there is NO converse check for battery_verdict=='clean' with nonzero lens findings (the schema's 'all zero is legal only with battery=clean' is only a comment, and reverify.mjs checks the fold, not finding-count/verdict consistency). A synthesis writing battery_verdict='clean' while lens artifacts carry P1/P2 findings would not be caught by the assembler's own guard, letting a clean verdict (cap=industrial) ship over suppressed findings. This is gaming-lens (S2) territory, flagged not proven here.

## lens: ?  (seed_hit=True, off_map=~40% — the seed named a preset-specific artifact_root/harness question; beyond it I proved the reverify-command failure is universal across all three shipped kinds (skill r1c, plugin r2d), traced the never-shipped-kit contradiction back to the schema's own 'never leave the factory' clause, and exercised the downstream dsh-acceptance-badge plugin's reverify=yes overstatement — none of which the seed pointed at.)

### [P2] /Users/vince/playground/dsh-tool-creator/docs/ACCEPTANCE-MANIFEST.md:6-8 & 34-67 (re-verify contract) vs /Users/vince/playground/dsh-tool-creator/src/validators/assemble_manifest.py:506-528 (emitted commands) and /Users/vince/playground/dsh-tool-creator/tools/reverify.mjs:417-436 (runs them, no kit/preset skip)
- claim: The acceptance-manifest standard's headline promise — 'the deterministic slice is re-runnable by anyone who holds the directory: node tools/reverify.mjs <artifact-dir> ... re-proved on rc.7, all checks green' — is unreachable in the documented default (full) mode for EVERY real shipped artifact. Two of the three emitted reverify commands (validate-dossier, validate-structure) invoke python3 validators/... over artifacts/evidence-dossier.json and artifacts/structure-contract.json, i.e. the pipeline-internal kit that the schema itself says 'never leave[s] the factory' (reverify.mjs:42) and is never placed inside any artifact tree. For a preset the harness command additionally escapes the shipped tree via ../evals/, which BUILD.md §1 deliberately keeps out of the install unit. reverify.mjs skips only the harness-path HASH check for presets; it still executes all three commands and returns exit 1. The KIT_STAGING_LIMIT / PRESET_WORKSPACE_HARNESS_LIMIT prose in limits[] discloses the symptom, but the doc's value section states the green-reproof claim without that caveat and the tool does not act on the disclosure, so a holder following the documented invocation gets a false-RED indistinguishable from tampering.
- repro: cd /Users/vince/playground/dsh-tool-creator; run `node tools/reverify.mjs /Users/vince/playground/tool-creator-runs/r1c/build` (skill), `.../r2d/build` (plugin), and `.../r3/build/preset` (preset). Then simulate the only distribution channel (BUILD.md §3.4 copy-only): `cp -R /Users/vince/playground/tool-creator-runs/r3/build/preset /tmp/installed && node tools/reverify.mjs /tmp/installed`.
- expected: Per the standard, a holder/installer re-runs the shipped verifier and gets 'all checks green' (exit 0), or the exact check that drifted.
- observed: skill r1c and plugin r2d: exit 1, cmd:validate-dossier and cmd:validate-structure FAIL ('can't open file .../validators/validate_report.py: No such file or directory'); harness passes (in-tree). Real preset r3 in-workspace: exit 1 (validate-* fail). Preset after the copy-only install: exit 1 with ALL THREE commands failing — cmd:harness got 127 ('bash: ../evals/run_harness.sh: No such file or directory'), plus both validate-* — so a copy-installed preset has ZERO runnable command re-proof; only `--skip-commands` (hash-only, exit 0) works. No test covers this: test/acceptance.test.mjs's green case uses hand-crafted toy commands, never the validate-dossier/validate-structure commands assemble_manifest.py actually emits, so the failure is default-missed.

### [P3] /Users/vince/playground/dsh-tool-creator/src/validators/assemble_manifest.py:404-417 (artifact_root), 461-499 (build_reverify preset branch skips harness membership), 420-454 (detect_identity)
- claim: artifact_root() checks has_marker(build/) FIRST and returns build/ on any hit, only descending to build/preset/ when build/ has no marker — so it silently mis-locates the artifact tree on two off-spec-but-plausible layouts, with no violation raised. (a) A stray marker file (package.json/preset.yml/agent.cordis.yml/SKILL.md) at build/ root while the real preset lives at build/preset/ makes the assembler hash the ENTIRE workspace build/ and mis-classify a preset as a 'plugin'. (b) A flat-built preset (composition + harness directly under build/, no preset/ subdir) is accepted as kind=preset and ships its own evals/run_harness.sh INSIDE the install unit — the exact BUILD.md §1 leak the preset branch exists to prevent — yet no guard fires, because build_reverify skips the harness-membership check whenever kind==preset. The mechanical assembler whose stated ethos is 'REFUSE where a value cannot be derived' here silently derives the wrong tree and kind.
- repro: Import the module and exercise directly (proven): with build/package.json present and build/preset/{preset.yml,agent.cordis.yml} present, `artifact_root(build)` returns build/ and `detect_identity` returns ('stray-helper','plugin','0.0.0') with []=no violations. Separately, run a full `assemble()` on a workspace whose build/ holds preset.yml+agent.cordis.yml+evals/run_harness.sh flat (no preset/ subdir): it returns violations==[] with kind='preset', 'evals/run_harness.sh' present in artifact.files, and harness argv ['bash','evals/run_harness.sh'].
- expected: For a preset, either the tree root is unambiguously build/preset/ or an ambiguous/mislaid layout is refused/disclosed; and the harness must never ship inside the preset install unit (BUILD.md §1) — a violation the preset branch claims to enforce.
- observed: Both cases assemble green with no violation: (a) preset silently manifested as a 'plugin' over the whole workspace build/ (harness, install.sh, fixtures all hashed as plugin files); (b) flat preset ships its harness in-tree with harnessPath='evals/run_harness.sh' and no leak detected. Requires an off-normal build layout (stray marker / flat build), so lower-probability, but it is a genuine silent-failure fragility with zero guard in a component built to refuse-or-disclose.

### [P3] /Users/vince/playground/tool-creator-runs/r2d/build/lib/acceptance.js:450
- claim: The dsh-acceptance-badge plugin (a shipped pipeline artifact that consumes the standard) derives its reverify signal as `manifest.reverify.commands.length > 0 ? 'yes' : 'no'` — a pure presence-of-commands check, not any actual re-verification. It therefore stamps reverify=yes on artifacts whose commands provably fail, overstating re-verifiability to downstream directories/marketplaces.
- repro: cd /Users/vince/playground/tool-creator-runs/r2d/build; `node -e "const{summarize}=require('./lib/acceptance.js');const fs=require('fs');console.log(summarize(JSON.parse(fs.readFileSync('/Users/vince/playground/tool-creator-runs/r3/build/preset/acceptance-manifest.json','utf8'))))"`.
- expected: A reverify=yes badge should mean the shipped verifier actually re-proves the artifact (exit 0) on a holder's host.
- observed: Output: 'News Collector preset effective=candidate ... reverify=yes' — for the very preset whose reverify commands all fail (exit 1) once installed. The plugin's own preset-not-reverifiable fixture uses a fabricated 0-command preset to reach reverify=no, a shape the real pipeline never emits (it always emits 3 commands), so every real preset gets reverify=yes. Lives in a produced artifact rather than the core pipeline, hence P3, but concretely demonstrates the overstated claim propagating downstream.

- FLAG @ /Users/vince/playground/dsh-tool-creator/src/preset/charter.md close-out fold + dsh-suite consumption path (not present in repo): Finding 1 assumes external holders never obtain the pipeline kit. I could not locate any documented 'stage validators/ + artifacts/ into the artifact root, then reverify' flow for an external directory (dsh-suite), and the dossier/structure-contract are explicitly the snake_case files that 'never leave the factory'. If some unshipped dsh-suite adapter stages the kit before reverify, the validate-* half of Finding 1 would be governed for that consumer (the preset harness-escape half would still stand). Worth confirming against the real dsh-suite integration, which is outside this repo.

## lens: ?  (seed_hit=True, off_map=~40% of effort went off-seed. Calibration checks that HELD in the target's favor: (1) differential-battery.md B15 corpus/trigger counts (30+30 golden pairs in 10 classes; trigger 30 cases = 20 pos/10 neg, 30/30 agreement) match r1c build ground truth exactly (build/fixtures, build/evals/trigger-results.json). (2) T-D5's gate-red claim verified: r1c gate-logs/battery.attempt1.log shows validate_decision.py exit 1 rejecting effective_verdict='draft' vs required min-fold 'candidate' — the fold-forgery-rejected story holds. (3) T-D2 first clause \"toolFilter 白名单机械生效\" holds: the r1c composer child's actual tool surface is exactly ['read','structured_output','subagent'] — whitelist read + the two own-scope exempt tools; inherited tools ARE mechanically filtered. (4) Pre-ban \"9 helpers\" count holds: the r1 session store contains exactly 9 depth-2 helper sessions. Off-seed anomalies surfaced as flags: the r2 zipper-ran-on-a-plugin-target oddity, and the DECISIONS.md vs shipped-doc corpus-count mismatch (shipped doc is the correct one).)

### [P2] /Users/vince/playground/dsh-tool-creator/docs/evidence/differential-battery.md:10 (T-D2 row)
- claim: T-D2 states the instruction-level delegation ban's post-ban record is "禁令后 0/4、0/4、1(唯一一个是只读 ls,已被检测)" — i.e. exactly ONE post-ban helper, and it was a harmless read-only `ls`, and it was detected. Status banked, framed as the ban working with one benign exception.
- repro: Enumerate depth-2 (helper) sessions across the post-ban run stores and read each helper's dispatch task + actual tool surface:
1) for run in r1c r2: for each dir under ~/.dsh/sessions/--Users-vince-playground-tool-creator-runs-$run--/, `zstd -dc session.jsonl.zstd | head -1` and print delegationDepth/parentSession. r1c yields helper f0133a20 (parent 0b4c3919); r2 yields helper a3cda458 (parent c80da887). => TWO post-ban helpers, not one.
2) Read a3cda458's first agent/inbox text: task = "Run `ls -laR /Users/vince/playground/tool-creator-runs/r2` … `ls -laR /Users/vince/.dsh/.agent-presets/tool-creator` … then READ (cat) and return the FULL verbatim contents of … the SkillSpec … the Structure Contract … Every file under …/build/" — a recursive enumeration + verbatim exfiltration of the workspace build tree AND the confined preset internals (schemas/targets/validators), NOT an `ls`.
3) Read either helper's request/header `tools` and its sandbox/mode + approval/policy: both ran with the FULL unfiltered global surface (33 tools incl. bash, write, edit, purge, safe_delete, pipeline_stage, subagent, cc_plugin_install), sandbox=workspace-write, approval=never — the opposite of "read-only." Confirm f0133a20 and a3cda458 each executed 0 tool-calls: they refused only because they inherited the tool-creator conductor charter (agentPreset "tool-creator").
- expected: If the T-D2 claim were accurate: exactly one post-ban helper, running read-only, benign by capability.
- observed: At least TWO post-ban depth-2 helpers exist (r1c f0133a20, r2 a3cda458). Neither is "read-only": both carry the complete unfiltered global tool surface (bash/write/edit/purge/safe_delete/pipeline_stage/subagent/cc_plugin_install…), sandbox=workspace-write, approval=never — precisely the "helpers run UNfiltered, recursion hazard is real" case documented in packages/dsh-pipeline-executor/SPIKE-FINDINGS.md:101-104. The one the doc omits (r2 a3cda458) was tasked with full exfiltration of the confined preset internals + build tree, not an ls. Nothing leaked ONLY because both helpers happened to inherit the conductor charter and refused — a contingent instruction-layer save, not the "read-only" nature or the detection the doc credits. The T-D2 tally therefore undercounts breaches and mischaracterizes the material one, understating the demonstrated residual risk of the (honestly-disclosed) host-open door.

### [P2] /Users/vince/playground/tool-creator-runs/r1c/artifacts/battery-lens-gaming.json:151 + /Users/vince/playground/dsh-tool-creator/.loop-state/DECISIONS.md:208 vs src/manifest/prompts/battery.md:42-53
- claim: r1c ships reporting its delegation audit as CLEAN: battery-lens-gaming.json:151 "Delegation audit CLEAN … Role children did NOT delegate helpers"; DECISIONS.md:208 "prompt-level ban held 0/4 role children spawning helpers." The shipped compensating control for the host-open subagent door is the "mechanical battery-stage session-log detection" that a lens runs (battery.md:42-53), presented (via T-D2 "已被检测") as the safety net that catches any delegation.
- repro: 1) In the r1c ledger (/Users/vince/playground/tool-creator-runs/r1c/evidence-ledger.jsonl), battery attempt-2 childSessionIds include 0b4c3919. Read 0b4c3919's first inbox text: "Begin synthesis for stage \"battery\" (attempt 2)…" => 0b4c3919 is the battery SYNTHESIS child (a ledgered role child).
2) In the r1c session store, helper f0133a20 has parentSession=0b4c3919 => the synthesis delegated a helper (task: "ls -la … artifacts/"). So a role child DID delegate, contradicting the CLEAN verdict.
3) Compare createdAt: gaming lens (one of 1fd5a411/6f02a3cb/65d6b1e4) runs and writes its audit BEFORE synthesis; f0133a20 createdAt=1787059986258, spawned by synthesis AFTER all lenses finished. battery.md:42 assigns the delegation audit to "a lens with bash (evidence or gaming lens)" — lenses run in parallel and complete before synthesis, so the audit structurally cannot observe a helper the synthesis spawns afterward.
- expected: If the battery session-log detection were the reliable net the docs imply, a role child delegating a helper in r1c would be caught and r1c would not report CLEAN.
- observed: r1c reports delegation CLEAN / 0 helpers, but the synthesis child 0b4c3919 spawned helper f0133a20 and the pipeline never caught it — because the only detection pass runs inside a lens, before synthesis exists to delegate. The mitigation has a structural blind spot at the synthesis stage (the most-privileged battery agent — it reads every artifact and writes the decision record). This makes the T-D2 "已被检测" qualifier unreliable and r1c's shipped CLEAN delegation verdict factually false.

- FLAG @ /Users/vince/playground/tool-creator-runs/r2/evidence-ledger.jsonl (zipper stage) vs src/manifest/pipeline.manifest.json:103 (zipper targets:["skill"]): r2's request is an explicit plugin build ("请构建一个名为 dsh-acceptance-badge 的 dsh 插件(plugin)"), yet the r2 ledger shows a real `zipper` stage that dispatched child c80da887 (att1 exit0). The manifest says zipper is skill-only and should skip for non-skill targets (ledgered, gateExit=0, no dispatch). This looks like r2 predates the 0.1.5 targets-gating, so a superseded intermediate run — but if any live plugin run still dispatches zipper, the "plugin/preset skip zipper" claim is wrong. Off my Evidence lens (coherence/reality); worth a targeted check on r2d/R2d, the final plugin run. Not verified as a finding.

- FLAG @ /Users/vince/playground/dsh-tool-creator/.loop-state/DECISIONS.md:211 vs docs/evidence/differential-battery.md:29-30: Internal working-doc DECISIONS.md:211 reports the B15 corpus as "26+27 classed" and trigger battery "22+12", while the shipped differential-battery.md reports "30+30 分 10 类" and "触发电池 30 案例(20正+10负)30/30". The shipped doc matches r1c ground truth (I counted: 30 input+30 expected fixtures across 10 classes; trigger-results.json = 30 cases, 20 pos/10 neg, 30/30). So the SHIPPED doc is correct and DECISIONS.md is the stale/mismatched one (likely r1b-era numbers). No finding against the shipped artifact; noted only so a maintainer doesn't reconcile in the wrong direction.

## SYNTHESIS  verdict=breaches_found
reconciliation: Confirmed the one P1 as legitimate, not inflated: the gaming floorless-clean breach is mechanically grounded across three independently-read code paths (grep 'seed' in packages/dsh-pipeline-executor/lib/* = 0 hits; validate_decision.py never opens battery-lens-*.json; assemble_manifest.derive_verdicts has a breaches_found->zero-counts guard but no converse clean->nonzero-counts branch; reverify.verdictFoldError fails only effective>cap). The realistic trigger is sloth/hollow-lenses, not only malice, and the design's own SEED anti-false-negative gate that would block it is pure persona-text. Keep P1. One DOWN-RANK: coherence S1 (preset spec unsatisfiable in spec-grill) P2 -> P3 — a real doc-vs-doc contradiction but bounded to the pre-pipeline interrogation layer, producing no wrong PASS/REFUSE (the intake gate is an existence-check on the '# TOOL-CREATOR SPEC v1' header per the gaming flag, so components are never enforced) and spec-grill is 'preferred', not mandatory. One UP-RANK: coherence S3 (default-lens desync) P3 -> effective P2, because it conceals the exact coverage hole the default 3-lens set creates (see IF-4). Held P2s, two of them independently re-run by me: reality reverify-unreachable (r1c full-mode FAIL 88/90, skip-commands the only green path) and foundation no-shipped-adjudication-disclosure (grep of all three live manifests = 0 real hits). Foundation F1 (O-L3 forced floor) held P2 but its material bite is realized only via F2/IF-1 — standalone it is a governance mislabel with no wrong-acceptance consequence (borderline P3), elevated by the interaction. Evidence T-D2 (undercount) held P2 as an evidence-doc honesty defect (undercounts post-ban helpers and mischaracterizes the material one as read-only), though nothing leaked. The five remaining P3s (S2 comment says 24576 vs field 32768; S4 stale 'these two invariants'; gaming evaluator self-report; reality badge reverify=yes; reality artifact_root mislocation) are correctly P3 — genuine but low blast-radius; S2 in particular is effectively cosmetic since the field wins at runtime and only a comment-reader is misled.

### [X P1] gaming x foundation
- The hollow-lens gaming path (P1) produces a traveling acceptance-manifest that is byte-indistinguishable from an earned industrial pass AND structurally cannot disclose the weakness that makes the trust unearned. Gaming supplies the unguarded clean->industrial unlock (derive_verdicts has a breaches_found->zero-counts guard but NO converse clean->nonzero-counts guard; reverify's verdictFoldError only fails effective>cap; validate_decision never reads battery-lens-*.json; 'seed' has 0 hits in the executor lib so the anti-false-negative gate is pure persona-text). Foundation supplies the fact that the shipped manifest carries no capability_level/adjudicator/veto field at all. Neither lens alone sees the compound: gaming assumed an auditor could still catch the contradiction by reading batteryFindingsCounts, but if the gamer zeroes the lenses the counts contradiction vanishes too, and Foundation shows there is no adjudication caveat anywhere in the traveling evidence. Result: a maximally-trusted (effective=industrial) artifact whose evidence chain leaves zero trace that it was machine-self-adjudicated over a floorless clean gate.
- repro: Take the gaming P1 cheat-workspace but set every artifacts/battery-lens-*.json to {"findings":[],"flags":[]} and decision-record acceptance={re_audit_verdict:industrial,battery_verdict:clean,effective_verdict:industrial}. Run the three gates as the manifest declares: (1) python3 src/validators/validate_decision.py -> PASS; (2) python3 src/validators/assemble_manifest.py ... -> PASS, writes verdicts={battery:'clean',effective:'industrial',batteryFindingsCounts:{p1:0,p2:0,p3:0}}; (3) node tools/reverify.mjs build --skip-commands -> PASS. Then grep -iE 'capabilit|adjudicat|veto|human' acceptance-manifest.json -> 0 real hits, matching all three live shipped manifests (verified: r1c/r2d/r3 return zero adjudication disclosure; r1c's lone regex hit is the substring 'capability-uplift' inside a baselineDelta note). The gamed manifest is field-for-field indistinguishable from a genuine clean run.

### [X P2] gaming x reality
- The re-verification system has no configuration in which a gamed clean->industrial verdict is caught. Reality: full (default) mode reverify FAILS exit 1 on every real shipped artifact because the emitted validate-dossier/validate-structure commands invoke python3 validators/... over the pipeline-internal kit that 'never leaves the factory' and is never placed in any artifact tree. Because that failure is a false-RED indistinguishable from tampering, the only working invocation is --skip-commands (hash-only). Gaming: the sole verdict-consistency check, verdictFoldError, runs in BOTH modes but is one-directional (BATTERY_CAP.clean=2/industrial; it fails only when effective exceeds the cap), so battery=clean+effective=industrial with any batteryFindingsCounts passes. The compound neither lens states: the reality bug funnels every downstream re-verifier off the command path and onto the exact hash-only path where the gaming bug green-lights the gamed verdict. Full mode = unusable false-RED; skip mode = blesses the gamed clean. There is no mode that both runs and catches it.
- repro: Runnable on shipped data, executed: `node tools/reverify.mjs /Users/vince/playground/tool-creator-runs/r1c/build` -> FAIL (88/90; cmd:validate-dossier and cmd:validate-structure error "can't open file .../build/validators/validate_report.py: No such file or directory"). `node tools/reverify.mjs /Users/vince/playground/tool-creator-runs/r1c/build --skip-commands` -> PASS (86/86, hash-only). Read tools/reverify.mjs: verdict-fold is a 'shape' check (line ~367) that runs in hash-only mode; verdictFoldError (line ~295) fails only RANK[effective]>expected; BATTERY_CAP={clean:2,...}. Therefore the IF-1 gamed manifest (battery=clean,effective=industrial) passes verdict-fold under the one invocation (--skip-commands) that any real holder is forced to use.

### [X P2] evidence x reality
- The honestly-disclosed host-open subagent door relies on two mechanical mitigations, and on one real run BOTH are non-functional, collapsing the defense to a single contingent instruction-layer charter refusal. Evidence: the battery delegation audit is assigned to 'a lens with bash' (battery.md:42), lenses complete before synthesis is spawned, so a helper delegated BY the synthesis child is structurally unobservable to the audit; r1c reports 'Delegation audit CLEAN ... Role children did NOT delegate helpers' and its own enumeration of checked childSessionIds omits the attempt-2 synthesis child, so the most-privileged battery agent (synthesis reads every artifact and writes the decision record) is the exact blind spot. Reality: the independent reverify re-proof the standard promises as the fallback assurance FAILS exit 1 on that same r1c artifact. So a consumer who receives r1c can neither trust the '已被检测' detection (it missed the real delegation) nor independently re-verify (the documented command errors). Neither lens alone concludes the door has zero working mechanical mitigation on a shipped run; the synthesis does.
- repro: (1) `node tools/reverify.mjs /Users/vince/playground/tool-creator-runs/r1c/build` -> exit 1 (independent re-proof unavailable; verified). (2) grep -n -i 'delegat|CLEAN' /Users/vince/playground/tool-creator-runs/r1c/artifacts/battery-lens-gaming.json -> line 151 'Delegation audit CLEAN ... Role children did NOT delegate helpers'; the note enumerates checked childSessionIds (c3ea8a9c,6302922b,735b1408,ae2c6789 + attempt-1 children) and does NOT include the attempt-2 synthesis child 0b4c3919 (verified against the artifact text). (3) sed -n '98,104p' packages/dsh-pipeline-executor/SPIKE-FINDINGS.md -> confirms helpers run unfiltered, 'the recursion hazard is real'. Both mechanical layers fail on the same run; only the contingent conductor-charter refusal prevented a leak.

### [X P2] coherence x gaming x foundation
- The battery run at its own DEFAULT is blind to the classes of its own worst defects, and its persona conceals the blindness. The manifest default fanout is THREE lenses (coherence/gaming/reality); evidence and foundation are opt-in. But every load-bearing P2 in the finding union against this pipeline comes from exactly those two opt-in lenses: evidence (delegation-audit blind spot, T-D2 undercount) and foundation (O-L3 forced floor, missing adjudication disclosure). Meanwhile the persona delivered to synthesis (battery.md:26) asserts 'default: all five + synthesis' and 'The five lenses ... do not invent a sixth'. So a default production run (a) never runs the two lenses that catch its strongest defect-classes, and (b) is told by its own charter that all five ran, suppressing an honest coverage_gaps entry for the two that did not. Under the floorless-clean P1 (no mechanical floor forcing rigor), that same synthesis emits a confident 'clean' with understated gaps. The coherence default-lens desync, a P3 in isolation, is load-bearing here because it conceals precisely the structural coverage hole the 3-lens default creates.
- repro: grep -A4 '"lenses"' src/manifest/pipeline.manifest.json -> [coherence,gaming,reality] (3, default; $comment confirms 'evidence and foundation are opt-in'). sed -n '26p' src/roles/battery.md -> 'lenses[] - subset of the five (default: all five + synthesis)' (reinforced at battery.md:63). The contradiction is grep-provable. Map the finding union by lens: the two P2 finding-classes tagged lens=evidence and lens=foundation both require lenses absent from the default fanout -> a default battery cannot surface them regardless of model behavior. This is a structural coverage defect, not merely a behavioral one.

coverage_gaps: Voided lenses: none — all five ran as attackers. Tier limitation (same as the pipeline's own disclosed battery_independence_tier, and it applies to THIS synthesis too): instance-tier, same model family as the author; a shared-model blind-spot class (a defect every instance of the author model shares) is structurally invisible and no attacker crossed the model/vendor boundary, so N green survivorship on one model cannot see it. Verification depth: I independently RAN and confirmed the reality reverify-unreachable P2 (r1c full FAIL / --skip-commands-only PASS) and the foundation no-disclosure P2 (grep of the three shipped manifests). I confirmed the gaming P1's mechanical preconditions by reading the code paths (seed absent from executor lib; validate_decision reads no lens artifacts; reverify's fold one-directional; assemble_manifest has no clean-branch) but did NOT execute the full 3-step crafted cheat-workspace end-to-end — the P1 rests on read code, not a re-run exploit. The evidence findings' zstd session-store parent-chain forensics I corroborated only against the r1c CLEAN note's own enumeration (which omits the attempt-2 synthesis child), not by decompressing session.jsonl.zstd myself. Not covered: the T-D7 47%-cost-cut arithmetic (needs r1c/R2/R3 session token logs, outside quick reach); whether an unshipped dsh-suite adapter stages the pipeline kit into the artifact tree before reverify (would govern the validate-* half of IF-2 and the reality P2 for that specific consumer) — that integration lives outside this repo; and the differential-battery's live token/cost numbers, which were not independently measured.