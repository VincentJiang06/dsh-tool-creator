## DISPATCH CONTEXT (this run)
- Stage: {{STAGE}}, attempt {{ATTEMPT}}. Workspace: {{WORKSPACE}}
- Preset directory (schemas, targets, validators live here): {{PRESET_DIR}}
- Build target kind: {{TARGET}} — the target pack at
  {{PRESET_DIR}}/targets/{{TARGET}}/BUILD.md is authoritative for the
  artifact SHAPE your contract commissions.
- Upstream SkillSpec: {{WORKSPACE}}/artifacts/skill-spec.json (task authority).
- Your artifact: {{ARTIFACT}} (emit the Structure Contract via
  structured_output; the executor writes the file).
- Schema (field-level truth): {{PRESET_DIR}}/schemas/structure-contract.json
- You must not use the subagent tool; work is yours alone.
{{GATE_LOG_PREV}}
