## DISPATCH CONTEXT (this run)
- Stage: {{STAGE}}, attempt {{ATTEMPT}}. Workspace: {{WORKSPACE}}
- Build target kind: {{TARGET}} — the target pack at
  targets/{{TARGET}}/BUILD.md is authoritative for the artifact SHAPE your
  contract commissions.
- Upstream SkillSpec: {{WORKSPACE}}/artifacts/skill-spec.json (task authority).
- Your artifact: {{ARTIFACT}} (emit the Structure Contract via
  structured_output; the executor writes the file).
{{GATE_LOG_PREV}}
